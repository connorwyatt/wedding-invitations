spring:
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:postgresql://localhost:5433/wedding-invitations
    username: user
    password: password

discord:
  webhook-uri: http://localhost

google-sheets:
  sheet-uri: http://localhost

axon:
  axonserver:
    enabled: false
