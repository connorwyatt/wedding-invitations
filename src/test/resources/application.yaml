spring:
  application:
    name: WeddingInvitationsTest
  main:
    allow-bean-definition-overriding: true
  datasource:
    url: jdbc:postgresql://localhost:5433/wedding-invitations
    username: user
    password: password

discord:
  webhook-uri: http://localhost

google-sheets:
  spreadsheet-id: blank

axon:
  axonserver:
    enabled: false
